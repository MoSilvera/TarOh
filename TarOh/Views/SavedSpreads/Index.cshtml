@model IEnumerable<TarOh.Models.SavedSpread>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrdinalId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CardDirection)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Card)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrdinalId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CardDirection)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Card.CardId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SavedSpreadId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SavedSpreadId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SavedSpreadId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
